// @syncrypt/provider-filesystem — StoragePort over a local directory plus a
// filesystem VaultPort adapter (RFC-0006; the deterministic test backend).

export * from "./storage.js";
export * from "./vault.js";
