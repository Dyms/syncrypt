# UI / UX Documentation

UX principles and client UI specifications (settings, sync status, the
human-readable sync log, conflict resolution flow, Safe Sync confirmations).

Guiding rule: **Syncrypt should never surprise the user** — the UI must always
make the current state and the reason for every action visible and understandable.
Populated alongside the Obsidian client (M4).
